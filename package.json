{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "restart": "lerna run tsc && lerna bootstrap --hoist && lerna run start"
  }
}
